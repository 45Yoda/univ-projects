/* #include "Graph.h"

// A utility function to add an edge in an 
// undirected graph. 
void Graph::addEdge(vector<int> adj[], int u, int v)
{
    adj[u].push_back(v);
    adj[v].push_back(u);
}


Graph::Graph() {

    // Create Intersections here
 
    
    */
/*
    // from 0 to number of intersections
    addEdge(city.street, 0, 1);
    addEdge(city.street, 0, 4);
    addEdge(city.street, 1, 2);
    addEdge(city.street, 1, 3);
    addEdge(city.street, 1, 4);
    addEdge(city.street, 2, 3);
    addEdge(city.street, 3, 4);
    addEdge(city.street, 3, 5);
    addEdge(city.street, 4, 5);
  */  
/*
    
} */