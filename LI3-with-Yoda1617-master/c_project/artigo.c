#include "./headers/artigo.h"#include "./headers/global.h"#include <string.h>#include <stdio.h>#include <stdlib.h>struct artigo {	char* titulo; //atualizar a cada revisao	int n; //nr de vezes que artigo aparece	char** timestamp; //guarda todos os timestamps por ordem de revisao	long* revId; // id das revisões por ordem de revisao	long bytes; //atualizar a cada revisao	long words; //atualizar a cada revisao};Artigo init_Artigo (int size) {	Artigo novo = (Artigo) malloc(sizeof(struct artigo));	int i;	novo->titulo=NULL;	novo->n=0;	novo->timestamp=malloc(size *sizeof(char*));	novo->revId=malloc(size *sizeof(long));	for(i=0; i<size;i++){		novo->timestamp[i]=malloc(MAXSIZE);	}	novo->bytes=0;	novo->words=0;	return novo;}char* getTitulo (Artigo a) {	return a->titulo;}void setTitulo(Artigo a, char* title){	a->titulo = title;}int getN(Artigo a){	return a->n;}void incrN(Artigo a){	 a->n+=1;}void getTimeStamp(Artigo a, char* t[]){	int b=getN(a),i;	for(i=0;i<b;i++){		t[i]=strdup(a->timestamp[i]);	}}void setTimeStamp(Artigo a, char* times,int pos){	a->timestamp[pos] = times;}void getRevId(Artigo a,long c[]){	int b=getN(a),i;	for(i=0;i<b;i++){		c[i]=a->revId[i];	}}void setRevId(Artigo a,long id, int pos){	a->revId[pos]=id;}long getBytes(Artigo a){	return a->bytes;}void setBytes(Artigo a, long bytes){	a->bytes=bytes;}long getWords(Artigo a){	return a->words;}void setWords(Artigo a, long words){	a->words=words;}void freeArt(Artigo a){	int i = 0;	int n = a->n;	free(a->titulo);	for(i=0;i<n;i++){free(a->timestamp[i]);}	free(a->timestamp);	free(a->revId);	free(a);}